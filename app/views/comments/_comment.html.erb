  <hr>
  <h5><%= comment.user.name %> </h5>
  <p><%= comment.content %> </p>
  <p class="time"><%= comment.created_at.strftime("%b %d, %Y") %></p>

  <%= link_to 'Delete', [comment.post, comment], method: :delete %>